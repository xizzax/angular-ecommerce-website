// define the interface based on the data inside the object
export interface IceCream {
    name: string;
    price: number;
    quantity: number;
    imgurl: string;
    iconurl: string;
    description: string;
    flavor: string;
    allergens: string[];
    rating: number;
}